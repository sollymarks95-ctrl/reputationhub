name: Deploy to Vercel
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: |
          RESULT=$(curl -s -X POST "https://api.vercel.com/v1/integrations/deploy/prj_Kziy22RlbG1OFUJkWAA8SC3Gn9U5/08k6QXAegy")
          echo "Result: $RESULT"
          echo "✅ Deploy triggered"
